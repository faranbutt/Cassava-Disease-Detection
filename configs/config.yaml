# configs/config.yaml
defaults:
  - model: model1
  - train: train
  - data: data
  - _self_

# For full pipeline, we'll override model via CLI
run_full: false  # ← keep false; we trigger via CLI
debug: true      # ← keep true for fast test
debug_samples: 100